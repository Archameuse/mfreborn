import type { NextPage } from 'next'
import Layout from '../../components/Layout'

const Patches: NextPage = () => {
    return(
        <Layout title='Patches'>Patches page</Layout>
    )
}

export default Patches